import React from 'react';
import IndexCards from '../../../components/index-cards/IndexCards'


export default function Inventory() {
    return (
        <IndexCards path={"/inventory"} />
    );
}
